# 出题助手页面验收清单

环境：`https://127.0.0.1:8443/mo-agent/workspace/custom-app/2a09ccb91eb60dc8e21ed3f9b989d467/99998a8308a94e09bfc5901bb77cc200/5`

前置条件：主站已登录，`localStorage.token` 有效，根路径 `/pyapi` 已代理。使用测试用户及 `e2e-question-helper-` 前缀题库进行写入验证，完成后仅清理本次创建的数据。

## 页面交互（待真实环境验证）

- [ ] AC-1：打开已有 `markdown-exam` 消息，正文显示试题卡片；前后解释文字正常；其他应用及 `/app` 原版页面保持原样。
- [ ] AC-2：生成期间下载不可用；试题围栏未闭合时保持不可操作；明确标记中断的消息不可操作；只读消息不显示操作入口。
- [ ] AC-3：下载得到可打开的 `.xlsx`，内容、答案、解析与试题块一致；模拟 JSON/HTML 错误响应时显示失败提示，不下载文件。
- [ ] AC-4：teacher/admin 用户可见加入题库；其他角色不可见；权限请求失败有提示且可以重试。
- [ ] AC-5：打开弹窗，搜索题库、加载后续页、多选；切换搜索时已选题库仍保留，可点击移除；一页全被学校隔离规则过滤时仍能继续下一页。
- [ ] AC-6：新建测试题库后自动选中；导入请求只包含选中 ID 和试题正文；提交期间重复点击只发出一次请求，弹窗不能关闭。
- [ ] AC-6：创建或导入失败时显示反馈；导入失败保留选择；不将空题目列表标记为成功。到原题库页面核实题目确实存在。
- [ ] AC-7：导入成功后当前卡片显示成功状态且不能再次提交；刷新后的状态不作为去重凭据，后端当前没有接入幂等协议。
- [ ] AC-8：Network 中 URL 从域名根路径 `/pyapi` 发起，Authorization 使用当前主站 Bearer token。不要记录或分享真实 token。
- [ ] 手机宽度下卡片操作区换行，弹窗可滚动，选择与提交均可操作。

## 已执行的定向检查

- 解析与 API 适配测试：17/17 通过（mock 请求，不读写真实题库）。
- 相关 8 个 TypeScript 文件语法和语义检查：0 诊断。
- 三语 `questionHelper` 文案：23 个键一致且组件引用均存在。
- 默认 Jest 环境缺少 `canvas.node`，以上纯逻辑测试使用临时 Node Jest 配置与最小浏览器存储桩运行，未修改项目测试环境或安装依赖。
- 未运行 ESLint、开发服务或构建；未执行真实题库写入。整体 E2E 状态：PARTIAL，页面及外部服务联调待验证。
