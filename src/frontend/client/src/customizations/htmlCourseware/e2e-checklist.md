# 网页课件助手验收

入口：`https://127.0.0.1:8443/mo-agent/workspace/custom-app/696ca5a56608f08ac2f8a384146e329a/1b2c32648808418fa9665f8022f33e36/5`

前提：已配置模型和提示词，输出完整文档的 `html` 围栏；主站已登录；根路径 `/pyapi` 代理包含云盘上传接口。

## 已执行检查

- 初次接入的围栏解析、HTML 辅助函数、上传适配及试题回归：39 项测试通过。本次下载修复的课件模块定向测试：30 项通过，覆盖源码保存、下载原文、云盘读取和错误响应。
- 本次课件相关 9 个 TypeScript 文件语法／语义检查通过；三语 14 个键一致。
- 使用已有本地服务与独立 Chrome 配置执行页面检查，API、WebSocket、剪贴板、下载点击及新窗口句柄使用模拟数据；未向真实云盘上传数据。
- 已验证历史消息、前后 Markdown、脚本可运行且无法访问主站存储、部署前缀、上传结果复用、弹窗拦截、失败后重试、流式等待／完成、独立预览路由、复制链接及无效地址拦截。
- 下载修复另用真实 Chrome 下载行为执行 8 项检查：有缓存、刷新后及无缓存但云盘允许 CORS 的场景均实际保存了 HTML 文件，并核对文件名和原文内容；下载不产生新标签页，未捕获运行时异常。云盘响应仍为模拟接口，下载点击和文件落盘未模拟。
- 桌面／手机截图人工检查；手机预览适应消息宽度，页面无横向溢出。
- 未运行 ESLint、开发服务或构建。浏览器检查使用已运行的服务。

## 真实环境待验收

- [ ] AC-1：从上述定制入口生成课件，确认真实模型输出 `html` 围栏；普通 Markdown 和试题助手保持原有行为。
- [ ] AC-2：生成中不执行脚本；完整生成后可预览；中断、缺少结束标签或围栏未闭合的内容不可操作。
- [ ] AC-3：真实课件中的图表、动画、外部脚本和图片正常；手机可滚动查看。CDN、混合内容与跨域资源策略由浏览器及资源服务决定。
- [ ] AC-4：使用测试账号和标题前缀 `e2e-html-courseware-` 上传课件，检查请求地址、主站身份、云盘网页课件目录及返回文件；仅清理本次创建的测试文件。
- [ ] AC-5：实际浏览器点击“新页面打开”，确认上传结束后新标签页可访问；刷新仍可预览；复制链接后在允许访问云盘资源的环境中打开；下载文件可用浏览器打开。
- [ ] AC-5 下载异常：无本页源码缓存且云盘禁止 CORS 时给出重新从原对话打开的提示，不跳转或额外打开网页。
- [ ] AC-6：断网／登录失效时有失败提示；恢复后可重试；双击不重复上传；只读页面隐藏上传与下载入口。
- [ ] AC-7：切换三种语言与品牌主题，按钮和提示正常。

整体状态：PARTIAL。已完成模拟接口下的逻辑与页面验证，真实模型、云盘写入、新标签页完整网络链路及外部资源联调仍待验证。原云盘接口不属于 BiSheng 后端，未生成不适用的 BiSheng 统一响应格式 pytest 用例。
